# 心得体会 · hello-gpu 五日学习总结

> 综合 Day 1–5 笔记、实验与打卡材料。所有性能数字均以本机/虚拟机/云端真实输出为准，教程历史数字只作参考，不作成绩。

## 一、一条主线：把"好像很快"变成"可复现、可解释、可委托"

五天的学习其实只做了一件事：**建立一条从直觉到证据链的流水线**。

- **Day 1 跑通**：理解 GPU 并行模型与线程映射（`i = blockIdx.x * blockDim.x + threadIdx.x`），第一次亲眼看到"几千个线程同时干活"长什么样——并行不再是概念，是索引公式。
- **Day 2 计时**：先学会"测什么"，再学"怎么测"。warmup 甩掉冷启动，GPU Event 包住真正的 kernel 区间，同步后再读数，报 mean/median/min/p95/标准差；甚至故意写一个"只测 CPU enqueue"的错误版本并标成 INVALID——**先认识坏证据，才能建立好证据**。
- **Day 3 解释**：用 rocprofv3 的 Kernel Trace 回答"到底跑了几次 dispatch、每次多久"；用 Roofline 回答"它离哪条天花板更近"；学会把结论分级为实测/推测/未验证。发现 vector_add 是 memory-bound（AI≈0.083 FLOP/B，远小于 ridge），方向立刻清楚：改访存，不改计算。
- **Day 4 实现**：亲手写 Triton kernel——`program_id`、`offsets`、`mask`、`load/store`；用 N=31/32/33/1027 做边界测试；固定 N 只扫 BLOCK_SIZE 做单变量实验。
- **Day 5 委托**：把前四天的纪律打包成 TaskSpec（题目契约）、baseline（0 号选手）、Oracle（标准答案）、正确性门禁（拦"快但错"）和一套确定性工具，让 Agent 在"提候选→工具裁决→反思"的闭环里迭代，全程留痕。

回头看，Days 1–4 是在练"当一个可信的测量者"，Day 5 是在学"把可信测量者的判断权交给确定性工具，把执行权交给 LLM，同时让题目本身不可被篡改"。

## 二、印象最深的五个点

### 1. 没有可信计时，优化就是玄学

Day 2 最震撼的不是 GPU Event 多准，而是"错误的测量也能给出一个漂亮的小数字"——只包住一次异步 add、不等待 GPU 完成的 CPU 墙钟，测到的主要是 CPU 提交开销，却小得很有说服力。没有 warmup、没有同步、没有统计口径，任何加速比都可能是噪声或幻觉。这条教训直接长成了 Day 5 的 `bench_kernel`：warmup + GPU Event + median，且晋升还要**与 incumbent 交错配对计时**——连"噪声被当成加速"这条路也堵上了。从此我形成了一个习惯：看到任何性能结论先问四个问题——输入是什么、计时边界在哪、统计口径是什么、机器环境是什么。

### 2. 正确性必须先于性能

Day 4 写 kernel 前先写 CPU reference，跑边界测试；Day 5 把它升级成门禁：`compile_error → wrong_answer → ok → below_threshold` 层层独立返回，"快但错"的候选在第二层就被拦下，**根本没有资格进入性能比较**。这让我意识到："它跑得很快"这句话，只有在"它是对的"之后才配说出口。优化史上最贵的 bug 不是慢，是快而错——它会被当成成果写进报告。

### 3. 单变量实验是归因的底线

Day 3 教材刻意埋了一个坑：改 `stride` 命令行参数同时改变了地址排布、每线程循环次数、Grid Size 三件事，6.7× 的差距不能全部归因于合并访存。公平对照要固定线程数与循环次数，只改索引公式。Day 4 的 BLOCK_SIZE 扫描、Day 5 Agent 的 SOP（一次只改一个主要机制），都是同一条纪律：**归因之前，先确认只动了一个变量**。多变量齐动得出的结论，只能叫现象，不能叫解释。

### 4. 失败与负结果是资产，不是污点

这条体会是五天里最值钱的：
- Day 2 故意保存"错误计时"的结果，是为了给正确方法立参照物；
- Day 3 要求写下"证据—当前判断—下一步单变量实验"，负结果被显式当作下一步的输入；
- Day 5 的 `trajectory.jsonl` 注释原文是"失败也记——失败比成功更值钱"；R5 把 `block_size` 推到 8192 反而慢了 17%，这条失败记录阻止了以后盲目把 block 推上极限。

负结果告诉你边界在哪里；**丢掉负结果的实验记录，等于丢掉边界知识**。这也是为什么教程反复强调"未加速也是有效结果，如实保留失败轨迹"。

### 5. 人机分工的边界，比 Agent 本身更重要

Day 5 最清醒的结论出现在第 17 章正文：Agent 的强项是**执行 + 留痕**——给定方向空间，能快速产出变体并自动验证；弱项是**发明全新结构**（线上 fused MLP 对照实验里纯 Agent 贡献接近 0%）。教程里 2.19× 的加速来自"故意留头寸"的朴素 baseline，有效机制只是更大 `block_size` + 更高 `num_warps`，是 SOP 引导下的配置搜索，不是算法突破。

更值得记住的是"不篡改裁判"的架构：候选人碰不到 `task.json` 和 `reference.py`（sha256 preflightEvidence 管着哈希），AST 白名单与子进程隔离挡住污染路径，只有 `accept_candidate` 能写 `best.py` 和轨迹。**"LLM 可信"不是靠它自觉，而是靠架构让它没有机会作弊**。这套思路完全可以泛化：任何 AI 辅助工程里，判断权应当交给可审计的确定性环节，AI 只贡献提案，且提案必须经过同样的裁决通道。

## 三、我的错误档案（回顾）

1. 曾习惯用"单次最快数字"谈论性能——Day 2 之后改成"min 是可复现的最优，median 才是代表值，必须附协议"。
2. 曾打算把 stride 实验的差距直接归因于合并访存——Day 3 之后学会先检查"这个参数还改了什么"。
3. Day 5 差点想把教程的 0.705→0.322 ms（2.19×）当成自己的成果——正确做法是：自己机器上的 baseline 自己测，best 自己测，加速比自己算；**历史数字只作参考，不反推不抄写**。
4. 曾担心"Agent 没加速 = 打卡失败"——实际上被拒绝的候选、未达阈值的试探和 429 限流后的保留工作区，都是轨迹的一部分，如实报告就是合格证据。

## 四、每日一句话回顾

| Day | 主题 | 一句话 | 留给后续的资产 |
|---|---|---|---|
| 1 | 认识 GPU 并跑通 Vector Add | 线程映射就是并行 | SIMT 直觉与 Thread/Block/Grid 心智模型 |
| 2 | 学会可信计时 | 先证明测到的是 kernel 时间 | 计时协议 = 输入 + warmup + Event + 统计口径 |
| 3 | 解释性能瓶颈 | Roofline 定方向，Trace 给证据 | 证据分级与"先归因再优化" |
| 4 | 亲手实现 Vector Add | 正确性先行，单变量实验 | Triton 基本功与边界处理 |
| 5 | 让 Agent 接手优化 | 提/判分离，留痕即账本 | 契约化评测闭环与失败回退 |

## 五、会带走的方法论清单（可迁移）

1. **性能结论四要素**：输入契约 + 计时边界 + 统计口径 + 环境版本，缺一不可；任何数字不带这四样就是不完整证据。
2. **先 Roofline 后动手**：先算算术强度、判断 bound 类型，再决定改"算"还是改"搬"；memory-bound 先减启动/提访存并行度，compute-bound 才动计算路径。
3. **自动化之前先写裁判规则**：评测器、阈值、计时协议先于人写下来并和候选隔离；自动化的可信度 = 裁判不可篡改的程度。
4. **负结果进账本**：每次失败实验记录"改了什么、证据是什么、边界在哪"，负结果与正结果同样引用。
5. **对 AI 工具的态度**：给它白名单工具和不可篡改的门禁，让它执行与留痕；关键结构洞察自己保留——"LLM 负责理解，代码负责相信什么"。

## 六、下一步

- 用同一套闭环冲击 3–5×：masked-softmax（融合）、naive reduction / matmul（LDS、向量化）这类有明显头寸的算子；
- 把 profile 增强到利用率字段（`sm_saturation_pct`、rocprof 计数器），让"瓶颈信号"更接近代码行；
- 把"人给洞察 → Agent 快速验证并留痕 → 裁判把关"的模式迁移到 CS336 的训练侧优化（kernel 选型、显存策略、通信压缩），那里同样需要可信计时与正确性门禁。

---

> 结语：五天学到的不是"会写一个 kernel"或"会跑一个 Agent"，而是一套**诚实的性能工作方式**——数字只信工具、结论必须可复现、失败必须留痕、判断权必须交给确定性环节。这套方式在同一条 GPU 上成立，在训练集群上也成立，在人与 AI 协作的工程里同样成立。