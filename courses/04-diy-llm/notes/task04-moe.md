# DIY-LLM Task 4：混合专家模型（MoE）学习笔记

> 本笔记通过「助产婆学习法」（苏格拉底式追问）逐层推导而来：不是背结论，而是把动机、机制、难题一步一步自己"接生"出来。

## 0. 学习主线

本笔记对应 Stanford CS336 第 4 讲《Mixture of Experts》与 Datawhale《DIY-LLM》第 5 章《混合专家模型》。归入 CS336 Assignment A1（Basics，notes 01–04），是整个基础系列的最后一篇。

这一讲回答一个看似矛盾的问题：**想让模型参数更多、容量更大，但不想让每次计算的算力也跟着线性暴涨** —— MoE 就是答案。

参考资料：

- 参考笔记：https://github.com/zizhengwang2026/0-1LLM-Stanford-CS336-Language-Modeling-from-Scratch-/blob/main/notes/04-mixture-of-experts.md
- CS336 课程主页：https://cs336.stanford.edu/
- Datawhale DIY-LLM：https://github.com/datawhalechina/diy-llm

一以贯之的推理链（本篇先搭骨架，逐节展开）：

    稠密模型 FLOPs ∝ 参数量（每个 token 永远走完所有参数）
      -> MoE：一个路由器 + 一堆专家，稀疏激活
      -> 打分全算（便宜）、执行只算选中 K 个（贵）
      -> 路由公式 y = Σ Gᵢ·Eᵢ（TC vs EC）
      -> 偏科与"死专家"（利用率问题 = 头号难题）
      -> 负载均衡：辅助损失（推路由器）+ 容量控制（换可预测算力）
      -> 共享专家（稀疏里的稠密保留）
      -> 训练稳定性（路由离散不可微 → 高方差，需特殊手段）

## 1. 稠密的死结：FLOPs ∝ 参数量

要读懂 MoE，先站到它的对面 —— 稠密模型。

在普通 Transformer 里，一层参数约 12 d²（注意力投影 4 d² + FFN 8 d²）。FFN 是最大的那块，约占总参数 **2/3**（注意是 8/12，不是 3/4）。

**关键事实**：稠密模型里，**每个 token 都必然完整流过全部参数**。FFN 前面没有任何"开关/路由器"决定某个 token 走不走。因此：

> **稠密模型的 FLOPs ∝ 参数量。参数翻 4 倍，每个 token 的计算量也翻 4 倍，两者绑死在一起。**

这就是最大瓶颈：想扩容（更大模型 → 更强能力），只能**线性地**增加算力，成本高到不可持续。

> 助产要点：这一步最容易混淆"算了个 0 再丢弃" vs "压根不算"。稠密 FFN 里没有"跳过"，一切运算都被执行。

## 2. MoE 是什么：稀疏激活

MoE 把 Transformer 里单个 FFN 换成**多个并行的"专家" FFN**，再加一个 **Router / Gating** 决定每个 token 交给哪几个专家。每次前向通常只激活 **Top-1 或 Top-2** 个专家，其余"挂机"不参与计算。

图书馆比喻：一个"图书管理员"（路由器）指引每个 token 读者只去少数几个相关"专家书架"，而不是翻遍整个图书馆。

**核心公式**：

    y = Σ_{i∈Top-K} Gᵢ(x) · Eᵢ(x)

- `Gᵢ`：门控权重（对选中的 K 个做 softmax 归一化后的凸组合权重）
- `Eᵢ(x)`：第 i 个专家的 FFN 输出

## 3. 稠密 vs 稀疏：最重要的一层区分

| | 稠密 | 稀疏（MoE） |
|---|---|---|
| FFN 结构 | 单一 FFN | 多个专家，只激活 Top-K |
| 计算量 | ∝ 参数量 | 每 token 固定算 K 个专家 |
| 激活参数 | = 总参数 | << 总参数（很多专家闲置） |
| 扩容 | 参数↑ ⇒ 算力↑（线性） | 参数↑但 FLOPs 几乎不变 |

核心卖点：**用几乎不变的 FLOPs，把参数容量撑大好几倍。** 参数大、数据多、算力足时才最占优；小规模或资源受限可能不如稠密。

**两个必须分清的"计算"**（本篇最容易翻车，务必钉死）：

1. **路由打分** `h(x) = x·Wg`：**便宜**（`Wg` 只是 `[d_model, E]` 的小投影），但**必须对全部 E 个专家都算** —— 因为你得先知道所有分数，才能挑出 Top-K。
2. **专家 FFN 执行** `Eᵢ(x)`：**贵**（那两个大矩阵），但**只对被选中的 K 个专家执行**。其余专家的 FFN 从头到尾没被调用。

> 一句话：**打分要全算（便宜），执行只算 K 个（贵）。** 这是稀疏激活和 FLOPs 降下来的真正所在。
>
> 易错："软max 再 top-k" 与 "top-k 再 softmax" 对选中 K 个的权重结果相同（`exp(zᵢ)/Σ_K exp(zⱼ)`），但无论哪种写法，**没被选中的专家 FFN 都不执行**。

## 4. 专家与 FFN 的关系

每个专家本身就是一个独立 FFN（`Linear → 激活 → Linear`），负责特征变换与知识存储，通常占大部分参数。

进阶变体：

- **共享专家（DeepSeekMoE）**：一两个**不做路由、每个 token 都必过**的稠密专家，兜住通用知识、降低冗余。**本质上仍是稠密成分**，是"稀疏 MoE 里必须保留一截稠密"的妥协。
- **细粒度专家分割**：把每个专家拆成更小（如 0.25×）的 FFN，增加专家个数、分工更细。

## 5. 路由机制（Routing / Gating）

步骤：

1. 打分 `h(x) = x · Wg`（Wg 为可学习投影）
2. 取分数最高 K 个专家 → softmax 归一化 → 其余置零

两种主流路由：

| | TC（Token Choice，token 选专家） | EC（Expert Choice，专家选 token） |
|---|---|---|
| 机制 | 每个 token 通过分数选 Top-K 专家 | 每个专家挑选它想处理的 token |
| 优点 | 语义完整 | 负载天然均衡 |
| 缺点 | 易"偏科"（热门专家爆满） | 可能丢弃 token（丢的是某个专家本要处理的那段语义） |

非学习路由：**哈希路由（LSH）** —— 固定哈希映射，天然均衡、无需训练，但语义弱。

**可学习路由的坑**：

- 路由是**离散选择**（选哪 K 个），在反向传播里**不可微** → 路由器拿不到关于"猜得准不准"的直接梯度 → **梯度高方差、不对齐**。
- 条件计算下**梯度不对称**：专家只在被选中时才更新，路由器却一直更新。
- 浅层 token 表示不稳定 → 进一步放大梯度方差。

## 6. 负载均衡与辅助损失（头号难题）

**问题**：少数热门专家处理绝大部分 token，冷门"死专家"闲置 —— 偏科且低效。**偏科伤害的是"利用率"，但不动 FLOPs**（每 token 仍固定算 K 个）。

> 死专家的后果：吃不到 token → **收不到梯度 → 常年不更新**。参数摆在那，但训练根本不在训练它。**"花好大成本买来一堆专家参数，结果大部分闲置 = 参数白买了。"** 这就是头号难题。

三种手段：

1. **辅助负载均衡损失**（Switch Transformer 采用）：在训练 loss 里加正则项，**直接去推路由器（Wg）改它的路由偏好**，鼓励各专家收到的 token 量均匀。注意：它**不是**去改专家的输出权重。
2. **容量控制**：`C = ⌈(N/E) × capacity_factor⌉`，每个专家最多收 `C` 个 token。超容量的 token **不走专家 FFN、只走残差**（不产生该专家梯度）。丢掉的是一点点语义，换来的是**算力预算的可预测性**（GPU 不会因某个专家爆满而被拖成不确定地长等）。
3. **动态无辅助损失均衡（Loss-Free Balancing）**：给专家加基于历史负载的 bias 调分数，不引入额外损失。

DeepSeekMoE 具体：专家级 `L_ExpBal` + 设备级 `L_DevBal`（多设备通信平衡）+ 共享专家。

## 7. 训练与推理特点

**训练**

- 稀疏路由引入**离散决策** → 梯度方差高；浅层表示非平稳 → 路由器难训。
- DeepSeek V4 遇 Loss Spike，用「**前瞻路由**」（解耦路由更新、缓存分配）+ **SwiGLU 钳制**（`[-10, 10]`）稳定。
- 这与 Task 3 的稳定性三板斧一脉相承：路由器的离散选择 + 与专家错开的更新节奏，最容易在训练初期把模型推向数值不稳定。

**推理**

- 只激活少数专家 → FLOPs 恒定；专家可分布多设备、并行高效；显存占用因激活少而优。
- DeepSeek V3 用 FP8 传激活、BF16 算关键部分保稳定。

**微调**

- Kimi-K2（1.04T 参数，激活 32.6B）用 **LoRA + RL**，GPU 成本降到约 10%；大基座 + 小 LoRA RL 优于小模型全参 RL。

## 8. 代表模型

- **Switch Transformer**：Top-1 路由、Router Z-loss、超容量丢弃。
- **DeepSeek 系列**：DeepSeekMoE（细粒度分割 N→mN、激活 mK、共享专家、辅助损失、FP8）、DeepSeek V4（浅层 3 层 Hash MoE 替 Dense、前瞻路由 + SwiGLU 钳制）、DeepSeek-R1（高性能推理）。
- **Mixtral**、**Qwen1.5-MoE**（upcycling 稠密→MoE）、**OLMoE**（从零高效追稠密）、**Kimi-K2**（LoRA+RL）。

## 9. 一句话记住这一讲

MoE = 多个专家 + 一个路由器，稀疏激活。核心卖点是**容量大、单步成本几乎不变**。路由 `y = Σ Gᵢ·Eᵢ`，TC 语义好易偏科、EC 均衡丢 token。负载均衡是头号难题（辅助损失 / 容量丢弃 / 无辅助损失）。推理优势明显：激活少 → FLOPs 恒定、省显存、专家可分布式；DeepSeek 用细粒度 + 共享专家做到极致。

## 10. 我自己接生出来的整条链（合上笔记能复述）

1. 稠密模型 FLOPs ∝ 参数量（每个 token 永远走完所有参数）→ 扩容被算力绑死。
2. MoE 拆成"一个路由器 + 一堆专家"，稀疏激活。
3. **打分要全算（便宜），执行只算 K 个（贵）** → FLOPs 几乎不变、参数容量大好几倍。
4. 路由公式 `y = Σ Gᵢ·Eᵢ`，选中 K 个之间 softmax 凸组合，没选中的"0"只是数学写法（实际没算它）。
5. TC 语义好易偏科（token 挤爆热门专家，其余死专家），伤害**利用率**不是算力；EC 均衡但丢 token。
6. 死专家收不到梯度、常年冻着 → **参数白买了** = 头号难题。
7. **辅助负载均衡损失**推路由器（Wg）改偏好 —— 软的一腿。
8. **容量控制** `C=⌈(N/E)·factor⌉`，溢出绕残差 → 用一点点语文换算力预算的可预测性 —— 硬的一腿。
9. 共享专家 = 稀疏里的稠密保留，兜通用知识。
10. 路由离散不可微 → 梯度高方差；路由器快、专家慢又错开 → 训练初期易数值不稳 → 靠前瞻路由 + SwiGLU 钳制稳住。

## 11. 自测清单（合上笔记回答）

1. 稠密模型为什么 FLOPs ∝ 参数量？关键在哪个事实？
2. "算了个 0 再丢弃" vs "压根不算"，MoE 属于哪一种？为什么这决定了 FLOPs 降不降？
3. 路由打分和专家执行，哪个要全算 E 个、哪个只算 K 个？各自为什么？
4. 写出 MoE 核心公式，说明 `Gᵢ` 的含义（是混合全部还是只混选中 K 个？）。
5. TC 和 EC 各有什么病？偏科伤害的是算力还是利用率？
6. "死专家"为什么叫死？它收不收得到梯度？后果是什么？
7. 负载均衡辅助损失是推"路由器"还是改"专家输出权重"？为什么？
8. 容量控制丢的是什么、换到的是什么？为什么宁可丢也不让 GPU 等着？
9. 共享专家在 MoE 里扮演什么角色？它本质上是稠密还是稀疏成分？
10. 为什么说 MoE 路由是"离散决策、梯度高方差"？它拿不到什么样的反馈？
11. 路由器和专家的更新节奏有什么不同？叠在一起最怕什么？
12. 训练稳定技巧：DeepSeek 用了哪招？（前瞻路由 / SwiGLU 钳制范围？）

### 关键校正（自测易错点）

- **"MoE 把所有专家算一遍再置零"** —— 错。稀疏激活的本质是"压根不算没选中的专家"，否则 FLOPs 不降。
- **"偏科会导致过拟合 / 梯度爆炸"** —— 不准确。偏科是**利用率**问题（死专家闲置 = 参数白买），不是过拟合，也不是数值爆炸。
- **"负载均衡损失改专家输出"** —— 错。它推的是**路由器 Wg** 改路由偏好。
- **"容量控制丢的是算力"** —— 反了。它丢的是一点点**语义**（该专家本要处理的特征变换），换来**算力预算的可预测性**。
- **"路由打分也只能算 Top-K"** —— 错。打分必须对全部 E 个专家算，否则挑不出 Top-K；真正省 FLOPs 的是"执行专家 FFN"只算 K 个。

## 12. 一页速查表

    MoE 核心      y = Σ_{i∈Top-K} Gᵢ(x)·Eᵢ(x)
    门控打分      h(x) = x·Wg            （便宜，E 个全算）
    专家执行      Eᵢ(x)                  （贵，只算选中 K 个）
    稀疏激活      总参数大 / 激活参数小   → 容量与 FLOPs 解耦
    稠密 vs 稀疏  稠密 FLOPs ∝ 参数量；稀疏 FLOPs 恒定、容量可撑大
    TC vs EC      TC 语义好易偏科；EC 均衡丢 token
    头号难题      负载均衡：死专家闲置 = 参数白买
    软的一腿      辅助负载均衡损失 → 推路由器 Wg 改偏好
    硬的一腿      容量 C=⌈(N/E)·factor⌉，溢出绕残差 → 换算力可预测
    共享专家      每个 token 必过的稠密专家 = 稀疏里的稠密保留
    训练稳定      路由离散不可微 → 高方差；前瞻路由 + SwiGLU 钳制 [-10,10]
    代表模型      Switch(TC+Z-loss) / DeepSeekMoE / Mixtral / OLMoE / Kimi-K2
